app.registerClass('Search/Engine/FailEngine', {

    getName: function()
    {
        return "fail";
    },

    setSearch: function(search)
    {
        // it doesn't matter what is happening there
    }
});